using Cinemachine;
using UnityEngine;
using UnityEngine.InputSystem;

// namespace Game.Camera
// {
    // public class CameraController : MonoBehaviour
    // {
        /*[SerializeField] private CinemachineFreeLook _freeLookCamera;

        private bool _isCameraEnabled;

       private void Start()
    {
        _freeLookCamera.Follow = target; // Встановлюємо ціль камери на персонажа

        // Отримуємо посилання на CinemachineInputProvider з CinemachineCore
        CinemachineCore cinemachineCore = _freeLookCamera.GetCinemachineComponent<CinemachineCore>();
        inputProvider = cinemachineCore.GetInputProvider() as CinemachineInputProvider;
    }

    private void Update()
    {
        // Перевіряємо, чи натиснута права кнопка миші або дотик на екрані
        if (Mouse.current != null && Mouse.current.rightButton.isPressed ||
            Touchscreen.current != null && Touchscreen.current.primaryTouch.press.isPressed)
        {
            // Вмикаємо вхід для камери
            inputProvider.Priority = 10;
        }
        else
        {
            // Вимикаємо вхід для камери
            inputProvider.Priority = 0;
        }
    }*/
// }